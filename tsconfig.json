{
  "compilerOptions": {
    "declaration": true, // Generates corresponding '.d.ts' file.
    "outDir": "./dist"   // Output directory for the build files.
    // include other necessary compiler options as per your project requirements
  },
  "include": ["src/**/*"], // Adjust if your source files are located in a different directory
  "exclude": ["node_modules", "dist"]
}

